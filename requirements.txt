# ==============================================================================
# ScrapGPT - Python Dependencies
# ==============================================================================
# Production-ready FastAPI web scraping platform
# Python 3.11+ recommended
# ==============================================================================

# -----------------------------------------------------------------------------
# Core Framework
# -----------------------------------------------------------------------------
# FastAPI: Modern, high-performance async web framework
# - Automatic OpenAPI docs, dependency injection, Pydantic integration
fastapi~=0.115.0

# Uvicorn: Lightning-fast ASGI server
# - [standard] includes uvloop, httptools for best performance
uvicorn[standard]~=0.32.0

# -----------------------------------------------------------------------------
# Database & ORM
# -----------------------------------------------------------------------------
# SQLAlchemy: Python SQL toolkit and ORM
# - [asyncio] enables async database operations
sqlalchemy[asyncio]~=2.0.30

# asyncpg: Fast PostgreSQL driver for asyncio
# - Required for async SQLAlchemy with PostgreSQL
asyncpg~=0.30.0

# Alembic: Database migration tool for SQLAlchemy
# - Version control for your database schema
alembic~=1.14.0

# -----------------------------------------------------------------------------
# Data Validation & Configuration
# -----------------------------------------------------------------------------
# Pydantic: Data validation using Python type hints
# - Core dependency for FastAPI request/response models
pydantic~=2.10.0

# Pydantic-settings: Settings management with .env file support
# - Type-safe configuration from environment variables
pydantic-settings~=2.6.0

# Email-validator: Email validation for Pydantic EmailStr
# - Required for email validation in auth schemas
email-validator~=2.3.0

# Python-dotenv: Load environment variables from .env files
# - Used by pydantic-settings for local development
python-dotenv~=1.0.0

# Python-multipart: Form data parsing
# - Required for OAuth2 password flow (form login)
python-multipart~=0.0.20

# -----------------------------------------------------------------------------
# Authentication & Security
# -----------------------------------------------------------------------------
# Python-jose: JavaScript Object Signing and Encryption
# - [cryptography] backend for JWT token handling
python-jose[cryptography]~=3.3.0

# Cryptography: Fernet encryption for user-owned provider API keys
cryptography>=42.0.0

# Passlib: Password hashing library
# - [bcrypt] for secure password hashing
passlib[bcrypt]~=1.7.4

# Bcrypt: bcrypt hashing (pinned for passlib compatibility)
# - Must use 4.x, passlib doesn't support bcrypt 5.x yet
bcrypt~=4.2.0

# -----------------------------------------------------------------------------
# Web Scraping
# -----------------------------------------------------------------------------
# Requests: Simple HTTP library for Python
# - Synchronous HTTP client for simple scraping tasks
requests~=2.32.0
# BeautifulSoup4: HTML/XML parsing library
# - Extract data from HTML documents
beautifulsoup4~=4.12.0

# lxml: Fast XML and HTML parser
# - High-performance parser backend for BeautifulSoup
lxml~=5.3.0

# HTTPX: Async HTTP client
# - Used for async scraping operations
httpx~=0.28.0

# Playwright: Browser rendering for JavaScript-heavy sites (OPTIONAL)
# Not installed by default. Required only if render_mode=BROWSER is used.
# Install separately after pip install -r requirements.txt:
#   pip install playwright
#   python -m playwright install chromium
# playwright>=1.40.0

# playwright-stealth: Patches Playwright Chromium to evade bot detection (OPTIONAL)
# Removes navigator.webdriver, injects real Chrome fingerprints, etc.
# Install: pip install playwright-stealth
# playwright-stealth>=1.0.0

# camoufox: Stealth Firefox for bypassing Cloudflare JS challenges (OPTIONAL, RECOMMENDED)
# Firefox-based — much harder to fingerprint than headless Chromium.
# Install: pip install "camoufox[geoip]" && python -m camoufox fetch
# camoufox[geoip]>=0.4.0

# -----------------------------------------------------------------------------
# Background Jobs
# -----------------------------------------------------------------------------
# APScheduler: Advanced Python Scheduler
# - Used for watchdog cleanup
apscheduler~=3.10.0

# -----------------------------------------------------------------------------
# Rate Limiting
# -----------------------------------------------------------------------------
# SlowAPI: Rate limiting for FastAPI
# - Production-ready, based on flask-limiter
slowapi~=0.1.9

# LiteLLM: unified provider abstraction for BYOK AI calls
litellm>=1.0.0

# openpyxl: Read/write Excel 2010 xlsx files — used for styled XLSX export
openpyxl~=3.1.0

# -----------------------------------------------------------------------------
# Development & Testing
# -----------------------------------------------------------------------------
pytest~=8.3.0                  # Testing framework
pytest-asyncio~=0.24.0         # Async test support
# ruff~=0.8.0                  # Fast Python linter
# mypy~=1.13.0                 # Static type checker
